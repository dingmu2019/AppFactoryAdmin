# Checklist

- [x] **自动注册验证**: 使用新邮箱 `roger@mc-cwin.com` 和正确验证码登录，数据库 `auth.users` 和 `public.users` 中成功创建记录。
- [x] **密码一致性验证**: 检查新创建用户的密码是否与登录时使用的验证码一致。
- [x] **角色分配验证**: 确认新用户的初始角色被正确设置为 `user` (普通用户)。
- [x] **访问拦截验证**: 使用普通用户身份尝试访问首页或其它管理页面，系统应自动重定向回登录页或报错。
- [x] **前端提示验证**: 重定向回登录页后，页面应明确提示“无权访问本系统”。
- [x] **会话清除验证**: 确认提示无权访问后，用户的登录态已被彻底清除（调用了 `signOut`）。
- [x] **管理权限验证**: 确认 `admin`, `editor`, `viewer` 角色的用户访问不受影响。
